name: Scheduled Workflow

on:
  schedule:
    - cron: '0 0 1 3 *'  # On the first day of March at midnight UTC


jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6.0.2

      - name: Run Scheduled Task
        run: echo "This is a scheduled task running every Sunday."